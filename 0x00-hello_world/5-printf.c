#include <stdio.h>
/**
 * main - main method will print text to the consol
 * Return: the program will return 0 if run successful
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

